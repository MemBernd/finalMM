loginForm:
    Provide:
        username String
        password String
    Return:
        role    String
        ?or false

CSO landing page:
    carry username - String (maybe use sessionstorage)


CSO creating eventRequest:
    Provide:
        clientRecord - integer
        eventType   - String
        eventStartDateTime  - DATETIME
        eventEndDateTime    - DATETIME
        attendees       - int

        //added
        decorations - string
        parties - string
        photos - string
        breakfast - string
        drinks - string

        budget      - float
        status      - string
    Return:
        execution - String  ["success", "failure", "client doesn't exists"]

SCSO landing page:
    carry username - String
    show tasklist, therefore return:
        table with:
            idTask - int
            subject - String
            description - String
            priority - - String
            status - STring     (filter tasks to 'created')
            eventRecord - INT
            creator - String    (username of employee, in this case empty)
            assignee - String   (username of employee)

    task will be processed by accepting or rejecting
        provide:
            idTask - int
            decision - String ["accepted", "rejected"]

SCSO summary writing
    carry eventRecord - int
    provide:
        eventRecord - int (carried)
        eventType - String
        description - String
        attendees - int
        eventStartDate - DATETIME
        eventEndDate - DATETIME
        budget - float
        list of objects of preferences

SCSO search for client
        provide:
            name - String
        return:
            clientRecord - INT
            clientName - String //added 13.10
            company - String

SCSO create client:
    provide:
        name - String
        company - String
    Return:
        execution - String  ["success", "failure"]

FM tasklist:
    general tasklist.php

FM deciding over event request: -> fmGetEventFromTask.php
    //opens an task regarding an event request with status 2 (acceptedBySCSO)
    provide:
        idTask - int
    return:
        eventRecord - int
        eventType - String
        eventStartDateTime - Datetime
        eventEndDateTime - datetime
        attendees - int
        budget - decimal(10,2)
        clientName - String
        company - String



FM adding feedback: //carry the idTask from before
    provide:
        idTask - INT
        description - String
    Return:
        execution - String  ["success", "failure"]
(process task)

AM tasklist:
    general tasklist.php

AM process Request:
    provide:
        idTask - int
        decision - String ["accepted", "rejected"]
